# Requirements
- Node.js >= 22
- pnpm

# Build Steps
1. Run `pnpm run util`.
2. Select `firefox` and then `repackage`.
3. The output will be a `package-x-x-x.zip` file in the current directory.

# Directory Structure
- `extension`: The main source code of the extension.
- `scripts`: Build and packaging utilities.
